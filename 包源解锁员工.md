# 短信员工 — 包源解锁操作手册

## 终端名称
**包源解锁** — 负责 bowjwj.cc 号码包解锁 + 凯总巴西漏包处理

## 快速命令

```bash
# 单个包解锁
curl -sS -k -X POST "https://bowjwj.cc/api/phone-packs/{packId}/unlock" \
  -H "Authorization: Bearer $(cat ~/.hermes/state/bowjwj/.jwt)" \
  -H "Content-Type: application/json"

# 批量解锁 (仅对 reuseLocked=true 的包有效)
curl -sS -k -X POST "https://bowjwj.cc/api/phone-packs/bulk-unlock" \
  -H "Authorization: Bearer $(cat ~/.hermes/state/bowjwj/.jwt)" \
  -H "Content-Type: application/json" \
  -d '{"ids":["pack-id-1","pack-id-2",...]}'
```

## 解锁流程

### 1. 找到被锁的包
```python
# 从凯总 campaigns 获取所有关联的包 ID
GET /api/campaigns?backendInstanceId=c7ee7c4c-...&pageSize=200&page=1&sort=createdAt&order=desc
# 筛选 campaignId 含 "凯总" 的 → 提取 phonePackId

# 逐个检查 reuseLocked 状态
GET /api/phone-packs/{packId}
# reuseLocked=True → 需要解锁
```

### 2. 执行解锁
- **首选**: `POST /api/phone-packs/{id}/unlock` (单个解锁，最可靠)
- **备选**: `POST /api/phone-packs/bulk-unlock` (批量，速度快但只处理 reuseLocked=True)
- **并行**: 最多 20 并发，间隔 0.05s/条

### 3. 陷阱
- **重锁**: 卡在 "sending" 状态的 campaign 会持续重锁包。解锁后可能被重新锁定
- **余额不足失败**: 前端显示的"余额不足失败"来自 campaign 发送失败，API 不直接暴露此状态
- **批量解锁跳过**: bulk-unlock 会跳过 reuseLocked=false 的包（返回 skippedIds）

## 凯总巴西漏包处理

### 脚本
`/Users/kate/Desktop/计划管理监控.py`

### 规则
- campaignId 含 "凯总"
- launchStatus = launched/draft/scheduled/created → 发送
- Globe/Dito → yo家 Globe 通道 (8个)
- Smart/TNT → yo家 Smart 通道 (6个)
- 间隔 0.5s/条，锁文件防重叠

### 执行
```bash
python3 /Users/kate/Desktop/计划管理监控.py
```

## 通道余额 (2026-05-04)

### 正余额（可用）
| 通道 | 余额 |
|------|------|
| 十二 yo家 Globe | 9,605,673 |
| 二 yo家 Smart | 9,841,566 |
| GG家 AAA | 9,749,044 |
| GG家 BBB | 9,509,764 |

### 负余额（不可用）
| 通道 | 余额 |
|------|------|
| 十四 yo家 Globe | -1,630,918 |
| 十 yo家 Smart | -1,064,252 |
| 九 yo家 Smart | -95,763 |

## JWT 凭据
```bash
cat ~/.hermes/state/bowjwj/.jwt  # 7天过期
```

## API 基础
- BASE: `https://bowjwj.cc`
- BID: `c7ee7c4c-ce0a-49c9-880a-9315d07c07b6` (NN33 ph)
- 列表返回格式不统一: 有的 `{items:[]}`, 有的 `{data:[]}`

## 常见问题

1. **包一直重锁**: 对应的 campaign 卡在 "sending" 状态，需先处理 campaign
2. **campaign 卡 sending**: 通道余额不足导致，API 无法取消/删除/重发
3. **"仅超级管理员可解锁"**: 需要 SUPER_ADMIN 权限
4. **JSON 解析失败**: pageSize 太大导致响应截断，改用 pageSize=50
